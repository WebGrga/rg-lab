{
  "name": "rg-lab",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Public index and path router for Roko Grga's independent software experiments.",
  "engines": {
    "node": ">=20.19"
  },
  "scripts": {
    "dev": "vite",
    "check": "tsc --noEmit",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "7.0.2",
    "vite": "^8.3.0"
  }
}

